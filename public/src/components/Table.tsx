import { useEffect, useState } from "react";

// 適切な型を定義してください
type TableRowData = {
  id: number;
};

const Table: React.FC = () => {
  //const [rows, setRows] = useState<TableRowData[]>([]);

  //useEffect(() => {
  //    // データフェッチロジック
  //    // 例: API からデータをフェッチして setRows で状態を更新
  //}, []);

  return (
    //<table>
    //  //<thead>{/* ヘッダー行をレンダリング */}</thead>
    //  //<tbody>
    //  //      //    {rows.map((row) => (
    //  //      //        <tr key={row.id}>{/* データセルをレンダリング */}</tr>
    //  //      //    ))}
    //  //  </tbody>
    //</table>
  );
};

export default Table;
